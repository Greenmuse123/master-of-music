{
  "goal": "Initialize the Master of Music project as a Vite + TypeScript + Vitest + ESLint + Prettier skeleton with a Canvas2D bootstrap that opens a 480x270 canvas scaled to fit the viewport with nearest-neighbor scaling. Establish the directory structure declared in docs/03-TECHNICAL_ARCHITECTURE.md section 3.",
  "files": [
    "package.json",
    "tsconfig.json",
    "vite.config.ts",
    "vitest.config.ts",
    ".eslintrc.cjs",
    ".prettierrc",
    "index.html",
    "src/main.ts",
    "src/game.ts",
    "src/config/constants.ts",
    "src/engine/render/renderer.ts",
    "src/engine/render/renderer.test.ts",
    "src/engine/scene/scene.ts",
    "src/engine/scene/scene-stack.ts",
    "src/engine/scene/scene-stack.test.ts",
    "src/scenes/title-scene.ts",
    "package-lock.json"
  ],
  "constraints": [
    "TypeScript 5.x strict mode (all strict flags on, including noUncheckedIndexedAccess).",
    "Do not introduce a game framework (no Phaser, PixiJS, Excalibur).",
    "Canvas2D only; imageSmoothingEnabled = false; integer-only positioning.",
    "Renderer must keep aspect ratio 16:9 and use CSS letterboxing at non-16:9 viewports.",
    "Vitest configured with jsdom environment and a stubbed CanvasRenderingContext2D for unit tests.",
    "No external dependencies beyond: vite, vitest, typescript, eslint, @typescript-eslint/*, prettier, @types/node, jsdom.",
    "Run `npm install` as part of execution before reporting completion. The wrapper's verification[] does not run install; you must, so that the npm scripts in verification are runnable.",
    "package.json must define scripts: dev, build, preview, lint, typecheck (alias for `tsc --noEmit`), test, test:run.",
    "title-scene's enter() test can live inside src/engine/render/renderer.test.ts or src/engine/scene/scene-stack.test.ts; a separate src/scenes/title-scene.test.ts is not required as long as the total passing-test count is at least 3 across the listed test files."
  ],
  "acceptance": [
    "`npm install` succeeds.",
    "`npm run dev` opens a window showing a black 480x270 canvas centered with letterboxing.",
    "`npm run lint` exits 0.",
    "`npm run typecheck` (alias for tsc --noEmit) exits 0.",
    "`npm run test` exits 0 with at least 3 passing tests (renderer instantiation, scene-stack push/pop, title-scene enter).",
    "`npm run build` produces dist/ with no errors."
  ],
  "verification": [
    "npm run lint --silent",
    "npm run typecheck --silent",
    "npm run test --silent",
    "npm run build --silent"
  ],
  "sandbox": "workspace-write",
  "invokedBy": "master-of-music orchestrator (Phase 0)",
  "timeoutMs": 900000
}
